---
title: Adapter-Out 层
description: Tika 解析、Embedding、Milvus v3、BM25 索引、GridFS、时效字段存储
---

# 文档上传与摄入 —— Adapter-Out 层

## ApacheTikaDocumentParserAdapter

文件：`synapse-kb-adapter/.../out/parser/ApacheTikaDocumentParserAdapter.java`

```java
@Component
public class ApacheTikaDocumentParserAdapter implements DocumentParserPort {
    private final AutoDetectParser parser = new AutoDetectParser();

    @Override
    public String parse(InputStream inputStream, String fileName) {
        try (InputStream is = inputStream) {
            byte[] bytes = is.readNBytes(maxParserInputBytes);
            rejectDangerousPdfForms(bytes, fileName);

            Metadata metadata = new Metadata();
            metadata.set(TikaCoreProperties.RESOURCE_NAME_KEY, fileName);
            BodyContentHandler handler = new BodyContentHandler(maxParsedChars);
            parser.parse(new ByteArrayInputStream(bytes), handler, metadata, parseContext());
            return handler.toString();
        } catch (Exception e) {
            throw new DomainException("文档解析失败: " + fileName, e);
        }
    }

    private ParseContext parseContext() {
        PDFParserConfig pdfConfig = new PDFParserConfig();
        pdfConfig.setExtractAcroFormContent(false);   // 禁用 AcroForm
        pdfConfig.setIfXFAExtractOnlyXFA(false);      // 禁用 XFA
        pdfConfig.setExtractInlineImages(false);      // 不提取图片
        pdfConfig.setExtractActions(false);           // 不提取动作
        pdfConfig.setOcrStrategy(PDFParserConfig.OCR_STRATEGY.NO_OCR);

        ParseContext context = new ParseContext();
        context.set(PDFParserConfig.class, pdfConfig);
        return context;
    }
}
```

### 安全设计

PDF 可能包含恶意内容（JavaScript、表单动作等）。Tika 的 `PDFParserConfig` 禁用了这些功能，只提取纯文本内容。

## OllamaEmbeddingAdapter

文件：`synapse-kb-adapter/.../out/embedding/OllamaEmbeddingAdapter.java`

```java
@Component
public class OllamaEmbeddingAdapter implements EmbeddingPort {
    private final OllamaEmbeddingModel embeddingModel;
    private final Timer embeddingTimer;

    public OllamaEmbeddingAdapter(@Value("${ollama.base-url}") String baseUrl,
                                   @Value("${ollama.embedding-model}") String modelName,
                                   MeterRegistry meterRegistry) {
        this.embeddingModel = OllamaEmbeddingModel.builder()
            .baseUrl(baseUrl)
            .modelName(modelName)
            .timeout(Duration.ofSeconds(120))
            .build();
        this.embeddingTimer = Timer.builder("synapse.embedding")
            .register(meterRegistry);
    }

    @Override
    @CircuitBreaker(name = "ollamaEmbedding")
    @Retry(name = "ollamaEmbedding")
    @Bulkhead(name = "ollamaEmbedding")
    public float[] embed(String text) {
        return embeddingTimer.record(() -> {
            return embeddingModel.embed(text).content().vector();
        });
    }

    @Override
    @CircuitBreaker(name = "ollamaEmbedding")
    @Retry(name = "ollamaEmbedding")
    @Bulkhead(name = "ollamaEmbedding")
    public List<float[]> embed(List<String> texts) {
        return embeddingTimer.record(() -> {
            List<TextSegment> segments = texts.stream()
                .map(TextSegment::from).toList();
            return embeddingModel.embedAll(segments).content()
                .stream().map(Embedding::vector).toList();
        });
    }
}
```

### 弹性设计

- **`@CircuitBreaker`**：Ollama 连续失败时快速失败，避免拖垮系统
- **`@Retry`**：临时失败时自动重试
- **`@Bulkhead`**：限制并发调用数，防止资源耗尽
- **`Timer`**：监控 Embedding 耗时

## MilvusVectorStoreAdapter

文件：`synapse-kb-adapter/.../out/vector/MilvusVectorStoreAdapter.java`

使用 **Milvus v2 SDK**，操作 **v3 collection**（默认 `synapse_document_chunks_v3`）。

### v3 Collection Schema

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VarChar(64) | 主键 |
| `knowledgeBaseId` | VarChar(64) | Partition Key |
| `documentId` | VarChar(64) | 文档 ID |
| `documentName` | VarChar(256) | 文档名称 |
| `chunkIndex` | Int32 | 分块序号 |
| `chunkText` | VarChar(65535) | 分块文本 |
| `vector` | FloatVector(1536) | 向量 |
| `startPosition` | Int32 | 起始位置 |
| `endPosition` | Int32 | 结束位置 |
| `effectiveFromEpochDay` | Int64 | 生效日期（Epoch Day） |
| `effectiveToEpochDay` | Int64 | 结束日期（Epoch Day），永久有效时为 `Long.MAX_VALUE` |
| `lifecycleStatus` | VarChar(16) | `ACTIVE` / `SUPERSEDED` / `RETIRED` |
| `canonicalKey` | VarChar(256) | 规范标识 |
| `versionLabel` | VarChar(64) | 版本标签 |
| `authorityLevel` | Int32 | 权威等级 |
| `jurisdiction` | VarChar(128) | 适用区域 |
| `sourceType` | VarChar(16) | `LEGAL` / `POLICY` / `GENERAL` |

索引：
- `vector`：HNSW，COSINE 度量
- `knowledgeBaseId`、`documentId`、`lifecycleStatus`、`canonicalKey`、`sourceType`：TRIE
- `effectiveFromEpochDay`、`effectiveToEpochDay`：STL_SORT

### store()

```java
@Override
public void store(KnowledgeBaseId knowledgeBaseId, DocumentId documentId, String documentName,
                  List<DocumentChunk> chunks, List<float[]> embeddings, DocumentMetadata metadata) {
    List<JsonObject> data = toRows(knowledgeBaseId, documentId, documentName, chunks, embeddings, metadata);
    insert(v3CollectionName, data);
}
```

### deleteByDocumentId()

```java
@Override
public void deleteByDocumentId(KnowledgeBaseId knowledgeBaseId, DocumentId documentId) {
    client.delete(DeleteReq.builder()
        .collectionName(v3CollectionName)
        .filter("knowledgeBaseId == '" + safeFilterValue(knowledgeBaseId.value())
            + "' && documentId == '" + safeFilterValue(documentId.value()) + "'")
        .build());
}
```

**安全边界**：删除必须同时匹配 `knowledgeBaseId` 与 `documentId`，禁止跨知识库操作。

### updateDocumentMetadata() —— v1 不支持

```java
@Override
public void updateDocumentMetadata(KnowledgeBaseId knowledgeBaseId, DocumentId documentId,
                                    DocumentMetadata metadata) {
    throw new UnsupportedOperationException(
        "v1 不支持在线修改向量索引元数据，请通过重新摄入新版本完成时效变更"
    );
}
```

v1 不支持在线更新 Milvus scalar 字段。正确的时效变更必须通过重新上传新版本完成。

## MongoChunkSearchIndexAdapter

文件：`synapse-kb-adapter/.../out/persistence/MongoChunkSearchIndexAdapter.java`

使用轻量 BM25 实现：
- 中文按单字/双字分词
- 英文按词分词
- 维护倒排索引 `ChunkPostingDocument`
- 维护语料统计 `ChunkCorpusStatsDocument`

### ChunkIndexDocument / ChunkPostingDocument 字段

除基础字段（`knowledgeBaseId`、`documentId`、`chunkIndex`、`chunkText` 等）外，还包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `effectiveFromEpochDay` | long | 生效日期（Epoch Day） |
| `effectiveToEpochDay` | long | 结束日期（Epoch Day），永久有效时为 `Long.MAX_VALUE` |
| `lifecycleStatus` | string | `ACTIVE` / `SUPERSEDED` / `RETIRED` |
| `canonicalKey` | string | 规范标识 |
| `versionLabel` | string | 版本标签 |
| `authorityLevel` | int | 权威等级 |
| `jurisdiction` | string | 适用区域 |
| `sourceType` | string | `LEGAL` / `POLICY` / `GENERAL` |

### updateDocumentMetadata() —— v1 不支持

```java
@Override
public void updateDocumentMetadata(KnowledgeBaseId knowledgeBaseId, DocumentId documentId,
                                    DocumentMetadata metadata) {
    throw new UnsupportedOperationException(
        "v1 不支持在线修改关键词索引元数据，请通过重新摄入新版本完成时效变更"
    );
}
```

## GridFsDocumentContentStoreAdapter

文件：`synapse-kb-adapter/.../out/persistence/GridFsDocumentContentStoreAdapter.java`

```java
@Component
public class GridFsDocumentContentStoreAdapter implements DocumentContentStorePort {
    private final GridFsTemplate gridFsTemplate;

    @Override
    public String store(KnowledgeBaseId kbId, DocumentId docId, String fileName,
                        String contentType, InputStream content) {
        Document metadata = new Document()
            .append("knowledgeBaseId", kbId.value())
            .append("documentId", docId.value());
        ObjectId objectId = gridFsTemplate.store(content, fileName, contentType, metadata);
        return objectId.toHexString();
    }
}
```

GridFS 是 MongoDB 的文件存储系统，适合存储大文件（超过 16MB 的文档）。

## 存量数据迁移说明

旧 collection `synapse_document_chunks`（v1/v2）不包含时效字段。v3 collection `synapse_document_chunks_v3` 拥有完整的时效 scalar 字段。

**存量数据必须重新摄入或迁移到 v3**，否则检索时无法执行 `effectiveFromEpochDay` / `effectiveToEpochDay` / `lifecycleStatus` 硬过滤，时效治理不完整。
